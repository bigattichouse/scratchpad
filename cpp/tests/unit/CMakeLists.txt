# Unit tests CMakeLists.txt

# Domain tests
add_subdirectory(domain)

# Application tests  
add_subdirectory(application)

# Infrastructure tests (planned for future implementation)
# add_subdirectory(infrastructure)